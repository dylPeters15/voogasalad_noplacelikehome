/**
 * The ConditionsPane interfaces package specifies the externally-facing
 * interfaces for the ConditionsPane classes that are created in the factory
 * package. This allows the classes in the factory package to be hidden, so that
 * any dependencies between them do not affect classes that use them through the
 * interfaces.
 * 
 * @author Stone Mathers
 *
 */
package frontend.interfaces.conditionspane;