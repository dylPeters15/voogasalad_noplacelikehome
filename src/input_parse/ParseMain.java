package input_parse;

import backend.util.GameState;
import input_parse.InputDecoder.Type;

public class ParseMain {

	public static void main(String[] args) {
//		GameState gameState = new GameState();
//		Parser parser = new Parser(gameState);
//		parser.parse(Type.PREDICATE, "(hello) -> {return hello.getTurnNumber() == 0;};");
//		UserPredicate predicate = new UserPredicate();
//		System.out.println(predicate.getPredicate().test(gameState));
	}

}
